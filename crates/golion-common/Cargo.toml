[package]
name = "golion-common"
version = "0.0.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"], optional = true }
derive_more = { version = "2", features = ["full"] }
chrono = "0.4.44"
[features]
serde = ["dep:serde"]

[lints]
workspace = true
